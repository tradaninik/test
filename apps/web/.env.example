# Copy to .env and adjust. Local dev uses a SQLite file (no setup required).
DATABASE_URL="file:./prisma/dev.db"

# Generate with:  openssl rand -base64 32
NEXTAUTH_SECRET="dev-secret-change-me"
NEXTAUTH_URL="http://localhost:3000"

# Optional (Phase 2): enables OpenAI Vision food recognition + LLM coach
# OPENAI_API_KEY=""
